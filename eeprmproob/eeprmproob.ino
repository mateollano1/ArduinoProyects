#include<EEPROM.h>
String guardado = "";
void setup() {
  // put your setup code here, to run once:
  
  pinMode(A2, OUTPUT);
  tone(A2, 230);
  delay(3000);
  noTone(A2);

}

void loop() {
  // put your main code here, to run repeatedly:
  

}
